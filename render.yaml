staticSites:
  # ------------------------------------
  # ⚛️ FRONTEND (Static Site)
  # ------------------------------------
  - name: cyber-game-frontend
    buildCommand: |
      cd frontend
      npm install
      npm run build
    publishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: cyber-game-backend
          property: url

services:
  # ------------------------------------
  # 🐍 BACKEND (Python FastAPI)
  # ------------------------------------
  - type: web
    name: cyber-game-backend
    env: python
    region: ohio
    plan: free
    buildCommand: |
      cd backend
      pip install -r requirements.txt
    startCommand: |
      cd backend
      uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
